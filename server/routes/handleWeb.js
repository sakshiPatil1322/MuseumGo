// const express = require('express');
// const router = express.Router();
// const Event = require('../models/event'); // Import your Event model

// Route to display all events sorted by name
// router.get('/adminWebHandle', async (req, res) => {
//     try {
//         const events = await Event.find().sort({ evName: 1 }); // Sort events by name in ascending order
//         res.render('adminWebHandle', { events });
//     } catch (err) {
//         console.error(err);
//         res.status(500).send('Server Error');
//     }
// });

// module.exports = router;
