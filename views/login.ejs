<!-- views/signup.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signup</title>
    <style>
        /* Inline CSS for Signup Form Styling */
        body {
            margin: 0;
            padding: 0;
            font-family: 'Arial', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: url('/img/booking.jpg') no-repeat center center fixed;
            background-size: cover;
        }

        body::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: inherit; /* Use the body's background image */
            filter: blur(3px); /* Adjust blur intensity here */
            z-index: -1; /* Place behind content */
        }

        .signup-container {
            background-color: rgba(83, 71, 71, 0.85); /* Semi-transparent white background */
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 8px 2px rgba(0, 0, 0, 0.2);
            width: 90%;
            max-width: 400px;
            text-align: center;
            opacity: 30px;
        }

        .signup-form {
            display: flex;
            flex-direction: column;
            align-items: stretch;
        }

        .signup-title {
            margin-bottom: 20px;
            font-size: 24px;
            color: #edebeb;
        }

        .form-group {
            margin-bottom: 20px;
            text-align: left;
        }

        .form-group label {
            font-size: 14px;
            color: #edebeb;
            margin-bottom: 5px;
            display: block;
        }

        .form-group input {
            padding: 10px;
            border: 1px solid #5b5555;
            border-radius: 5px;
            font-size: 14px;
            width: 100%;
            box-sizing: border-box;
            transition: border-color 0.3s;
            background-color: rgba(83, 71, 71, 0.85);
            padding-left: 20px;
            color: #b5b4b4;
        }

        .form-group input:focus {
            border-color: #FEA116; /* Orange shade for focus */
            outline: none;
        }

        .signup-button {
            padding: 12px 20px;
            background-color: #FEA116; /* Your preferred orange shade */
            color: #fff;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .signup-button:hover {
            background-color: #e38d00; /* Darker shade on hover */
        }

        p{
            color: #edebeb;
        }
        a{
            color: #FEA116;
        }
        a :hover{
            color: #e38d00; 
        }
    </style>
</head>
<body>
    <div class="signup-container">
        <form class="signup-form" action="/user/login" method="POST">
            <h2 class="signup-title">Login</h2>
            <div class="form-group">
                <label for="email">Email:</label>
                <input type="email" id="email" name="email" required>
            </div>
            <div class="form-group">
                <label for="password">Password:</label>
                <input type="password" id="password" name="password" required>
            </div>
            <button type="submit" class="signup-button">login</button>
        </form>
        <p>Don't have an account? <a href="signup.ejs">Signup here</a></p>
    </div>
</body>
</html>
